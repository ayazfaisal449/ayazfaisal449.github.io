# Optional — contributions and achievements use public third-party APIs by default.
# GITHUB_TOKEN is only needed if you want GraphQL as a fallback for contributions.
GITHUB_TOKEN=

# GitHub username for contributions and achievements (defaults to faisalayaz447)
GITHUB_USERNAME=faisalayaz447

# Primary SEO URL (canonical). Netlify builds auto-use noindex; canonical still points here.
SITE_URL=https://ayazfaisal449.github.io
